diff --git a/src/main.c b/src/main.c
index 26b4c51..1d19284 100644
--- a/src/main.c
+++ b/src/main.c
@@ -271,7 +271,7 @@ readCoreXmlCallback (void *context,
     i += compReadXmlChunk ("</screen></core></compiz>", &offset, buffer + i,
 			   length - i);
 
-    if (!offset && length > i)
+    if (i > 0 && !offset && length > i)
 	buffer[i++] = '\0';
 
     ctx->offset += i;
diff --git a/src/metadata.c b/src/metadata.c
index d7a0184..a1c309c 100644
--- a/src/metadata.c
+++ b/src/metadata.c
@@ -286,7 +286,7 @@ readPluginXmlCallback (void *context,
     i += compReadXmlChunk ("</plugin></compiz>", &offset, buffer + i,
 			   length - i);
 
-    if (!offset && length > i)
+    if (i > 0 && !offset && length > i)
 	buffer[i++] = '\0';
 
     ctx->offset += i;
